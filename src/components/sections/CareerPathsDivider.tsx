import styles from "./CareerPathsDivider.module.css";

export default function CareerPathsDivider() {
  return (
    <div className={styles.wrapper}>
      <span className={styles.label}>career paths</span>
    </div>
  );
}
