import styles from "./PopularCompaniesDivider.module.css";

export default function PopularCompaniesDivider() {
  return (
    <div className={styles.wrapper}>
      <span className={styles.label}>popular companies</span>
    </div>
  );
}
